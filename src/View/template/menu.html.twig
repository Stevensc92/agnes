<!-- Header -->
<header>
    <div class="sticky-nav">
    	<a id="mobile-nav" class="menu-nav" href="#menu-nav"></a>

        <div id="logo">
        	<a id="goUp" href="{{ path('index') }}" title="Logo">Index</a>
        </div>

        <nav id="menu">
        	<ul id="menu-nav">
            	<li class="current"><a href="#home-slider">Index</a></li>
                <li><a href="#work">Our Work</a></li>
                <li><a href="#about">About Us</a></li>
                <li><a href="#contact">Contact</a></li>
                {% if app.username is empty %}
                    <li><a href="#signUpModal" data-toggle="modal">Inscription</a></li>
                    <li><a href="#loginModal" data-toggle="modal">Login</a></li>
                {% else %}
                    <li><a href="{{ path('logout') }}" class="external">Déconnexion</a></li>
                {% endif %}
            </ul>
        </nav>

    </div>
</header>
<!-- End Header -->

{% if app.username is empty %}
<div id="loginModal" class="modal hide fade" tabindex="-1" role="dialog" arira-labelledby="myLoginModal" aria-hidden="true">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
        <h3 id="myLoginModal">Connexion</h3>
    </div>

    <form action="{{ path('login') }}" method="post">
        <div class="modal-body">
            <label for="username">Pseudo :</label>
            <input type="text" id="username" name="username" value="{{ last_username }}" required/>

            <label for="password">Mot de passe :</label>
            <input type="password" id="password" name="password" required />
        </div>

        <div class="modal-footer">
            <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
            <button class="btn btn-primary" type="submit" name="login">Connexion</button>
        </div>
    </form>

</div>

<div id="signUpModal" class="modal hide fade" tabindex="-1" role="dialog" arira-labelledby="mySignUpModal" aria-hidden="true">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
        <h3 id="myLoginModal">Inscrivez-vous</h3>
    </div>

    <form method="post" action="{{ path('signUp') }}">
        <div class="modal-body">
            <label for="username">Pseudo :</label>
            <input type="text" id="username" name="signUpUsername" value="{{ lastUsername }}" required />

            <label for="password">Mot de passe :</label>
            <input type="password" id="passwod" name="signUpPassword" required />

            <label for="email">Adresse mail :</label>
            <input type="email" id="email" name="signUpEmail" required />
        </div>

        <div class="modal-footer">
            <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
            <button class="btn btn-primary" type="submit" name="signUp">Inscription</button>
        </div>

    </form>
</div>
{% endif %}
